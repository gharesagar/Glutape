<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background Color -->
    <solid android:color="@android:color/white"/>

    <!-- Border Color -->
    <stroke android:width="0.8dp" android:color="@android:color/black" />

    <!-- Round Corners -->
    <corners android:radius="2dp" />

</shape>